<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login/Register Page</title>
    <link rel="stylesheet" href="/css/login-regiseter.css">
</head>
<body>
    <div class="form-container">
        <div class = "message">
            <p></p>
        </div>
        <h2 id="loginText">Login</h2>
        <form action="/auth/login" method="post" id="loginForm"><!-- Replace with your actual login endpoint -->
            <label for="loginUsername">Username:</label>
            <input type="text" id="loginUsername" name="username" required>

            <label for="loginPassword">Password:</label>
            <input type="password" id="loginPassword" name="password" required>

            <button type="submit">Login</button>
        </form>

        <form action="/auth/register" method="post" id="registerForm" onsubmit="validatePasswordMatch(event)">
            <h2 id="registerText">Register</h2>
            <label for="registerUsername">Username:</label>
            <input type="text" id="registerUsername" name="username" required>
        
            <label for="registerEmail">Email:</label>
            <input type="email" id="registerEmail" name="email" required>
        
            <label for="registerPassword">Password:</label>
            <input type="password" id="registerPassword" name="password" required pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[!@#$%^&*]).{8,}" title="Must contain at least one digit, one lowercase letter, one uppercase letter, one special character, and be at least 8 characters long">
        
            <div class="password-hint">Hint: Password must contain at least one digit, one lowercase letter, one uppercase letter, one special character, and be at least 8 characters long.</div>
        
            <div class="password-strength" id="passwordStrength">Password Strength: <span id="strengthText"></span></div>
        
            <label for="confirmPassword">Confirm Password:</label>
            <input type="password" id="confirmPassword" name="confirmPassword" required>
        
            <button type="submit">Register</button>
        </form>

        <div class="switch-form" id="noAccount" style="display: block;">
            <p>Don't have an account? <a href="#" onclick="switchForm('register')">Register here</a></p>
        </div>
        
        <div class="switch-form" id="hasAccount" style="display: none;">
            <p>Already have an account? <a href="#" onclick="switchForm('login')">Login here</a></p>
        </div>
    </div>
    <script src="/JavsScript/login-register.js"></script>
    {{#if message}}
    <script>
        document.querySelector('.message p').innerHTML = "{{message}}";
    </script>
    {{/if}}
</body>
</html>
